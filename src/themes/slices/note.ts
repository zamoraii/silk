import type { EditorThemeClasses } from "lexical";

export const noteTheme: EditorThemeClasses = {
  // NoteNode uses direct class names in createDOM rather than theme
  // but we expose the token for consistency with the theme system
};
